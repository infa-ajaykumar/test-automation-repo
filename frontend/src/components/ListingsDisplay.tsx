import React from 'react';     import { Listing } from '../services/api';     import ListingCard from './ListingCard';         interface ListingsDisplayProps {       listings: Listing[];     }         const ListingsDisplay: React.FC<ListingsDisplayProps> = ({ listings }) => {       /* This component will be updated when loading/error states are handled in SearchPage */       /* if (listings.length === 0) {         return <p>No listings found.</p>;       } */       return (         <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))', gap: '20px' }}>           {listings.map((listing, index) => (             <ListingCard key={listing.url || index} listing={listing} />           ))}         </div>       );     };     export default ListingsDisplay;
