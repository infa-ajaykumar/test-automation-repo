import { defineConfig } from 'vite'; import react from '@vitejs/plugin-react'; // https://vitejs.dev/config/ export default defineConfig({   plugins: [react()],   server: {     port: 3000, // Standard port for frontend dev     strictPort: true, // Fail if port is already in use     proxy: {       // Optional: Proxy API requests to backend during development       // This avoids CORS issues if backend is on different port/domain       '/api': {         target: 'http://localhost:8000', // Assuming backend runs on 8000         changeOrigin: true,         rewrite: (path) => path.replace(/^\/api/, ''), // Remove /api prefix before forwarding       },     },   }, });
